Import pygame

size width, height = 400, 300

screen pygane.display.set_node(size)

clock pygame.time.Clock()

pygame.display.setIcaption("Желтый круг")

running = True

в Красим фом

screen.fill(pygame.Color('blue'))

circle width=

circle radius = 0

circle exists False # переменная указывает на существование круга circle.color = pygame.Color('yellow')

plus_radius pygane.USEREVENT 25 # номер нашего созданного события * таймер вызова события увеличения радиуса круга

pygame.time.set_timer(plus_radius, mis 100)

while running:

for event in pygame.event.get(): if event.type* pygame.QUIT:

running False • реакция на нажатие мыши

if event.type = pygame. MOUSEBUTTONUP:
    circle exists True

и красим фон и поверх него рисуем новый круг

screen.fill(pygame.Color('blue'))

circle radius

circle pos event.pos

pygame.craw.circle(screen, circle color, circle pos, circle redios, circle width)

• Событие увеличения радиуса круга

if event.type plus radius and circle exists:

circle redius 1

pygane.draw.circle(screen, circle color, irsin pos, circle radius, circis width)

Pygame.display.Lip

clock.tick (50)

Pygame.quit()
